{
  "name": "@akwe/backend",
  "version": "0.1.0",
  "private": true,
  "description": "AKWÈ — sync Supabase, IA vocale, reçus. Lead : AMINOU Malick.",
  "engines": { "node": ">=20.11.0" },
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run typecheck && npm run test"
  },
  "dependencies": { "@supabase/supabase-js": "^2.45.4" },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
